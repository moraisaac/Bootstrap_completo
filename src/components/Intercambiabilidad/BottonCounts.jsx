import React from 'react'

const BottonCounts = ({handleInter}) => {
    return <Button
        className="mx-2"
        onClick={handleInter}
    >
        Agregar Al carrito
    </Button>
}

export default BottonCounts